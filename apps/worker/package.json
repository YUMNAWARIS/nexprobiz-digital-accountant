{
  "name": "@fa/worker",
  "version": "1.0.0",
  "private": true,
  "description": "Receipt OCR queue consumer (\u00a737/\u00a738). Runs the worker entry of the @fa/api modular monolith.",
  "scripts": {
    "dev": "pnpm --filter @fa/api dev:worker",
    "build": "pnpm --filter @fa/api build",
    "start": "pnpm --filter @fa/api start:worker",
    "lint": "echo \"worker sources live in apps/api/src/worker (linted there)\"",
    "typecheck": "echo \"worker sources live in apps/api/src/worker (typechecked there)\"",
    "test": "echo \"worker tests live in apps/api (src/worker/__tests__ + test/receipts.test.ts)\""
  },
  "dependencies": {
    "@fa/api": "workspace:*"
  }
}
